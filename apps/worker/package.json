{
  "name": "@chaop/worker",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @chaop/protocol build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @chaop/protocol build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test dist/**/*.test.js",
    "dev": "pnpm --filter @chaop/protocol build && CI=true wrangler d1 migrations apply chaop-control --local --config wrangler.jsonc && CI=true wrangler dev --config wrangler.jsonc --var CHAOP_DEV_ALLOW_INSECURE:true --var AGENT_BOOTSTRAP_SECRET:local-dev-bootstrap-secret",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "@chaop/protocol": "workspace:*",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  }
}
