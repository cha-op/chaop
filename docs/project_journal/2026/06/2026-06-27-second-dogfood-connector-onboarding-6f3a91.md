---
id: 20260627-6f3a91
title: Second Dogfood Connector Onboarding
status: active
created: 2026-06-27
updated: 2026-06-30
branch: master
pr:
supersedes: []
superseded_by:
---

[ British English | [简体中文](2026-06-27-second-dogfood-connector-onboarding-6f3a91.zh-Hans.md) ]

# Second Dogfood Connector Onboarding

## Summary
- A second host is onboarded against the existing Cloudflare control plane, and Chaop can now perform bounded writes inside its own repository for self-hosted development.
- Its connector identity and private token, spool, upgrade-marker, and process-state paths are isolated from the temporarily unavailable first host.
- Deployment-instance names, domains, account identifiers, secrets, and local paths remain only in the private ops repository and ignored local state.

## Current State
- The local Node 24, repository-pinned pnpm, Rust, and Codex app-server prerequisites are available.
- The release connector build and configuration doctor passed.
- Connector bootstrap succeeded with a newly rotated Worker bootstrap secret, and the connector token is stored with user-only permissions.
- The connector and its managed app-server remain healthy in a pollable foreground session; the app-server listener is loopback-only and the connector holds an established Worker TLS/WebSocket connection without authentication or reconnect errors.
- The ordinary background wrapper was not used as final evidence because this Codex command runner cleans up detached child processes when a command session ends. This is a local session-ownership constraint, not evidence of a connector authentication failure.
- The headless LaunchDaemon is installed and active in the system domain, while the connector and managed app-server run as the ordinary host user. A controlled termination verified `KeepAlive`, graceful app-server cleanup, a clean connector exit, and restoration of the loopback listener and Worker TLS/WebSocket connection with new processes.
- An Access-authenticated manual Browser pass confirmed that the connector and managed app-server are visible and healthy. Local thread creation and a bounded managed turn were exercised. The tracked service-token smoke then passed direct health, bootstrap, usage-summary, same-origin asset, and real Chromium checks without failed browser responses.
- The first inventory produced a sharp D1 rows-written burst, then the measured daily total increased only from 1,242 to 1,277 while the bounded product flow completed. Code inspection showed that the connector can import up to 200 Host Sessions on first contact; the table and index writes explain the one-time burst, while unchanged later reports skip Host Session row updates.
- Read-only production D1 aggregation confirmed 217 Host Sessions discovered today for the new connector and no current-day inventory changes for the legacy connector. At roughly five D1 row mutations per Host Session insert, those rows explain about 1,085 of the measured 1,277 daily writes. The database contains 1,545 historical Host Sessions, showing that the top-N report limit is not a retention cap.
- The first connector restart after the replacement build occurred during a measured daily-write increase from 1,277 to 1,552. A later read-only aggregate found only 12 additional Host Sessions, so inserts alone do not explain the 275-row interval; existing inventory updates, sync/connector/app-server state, and telemetry samples can also contribute, and exact path attribution still needs query-meta instrumentation. Code inspection separately found that equal update timestamps reached top-N truncation through randomised `HashMap` order. The deployed follow-up sorts ties by session id. A first restart advanced the current connector from 229 to 236 stored sessions while 17 local rollout files had changed; an immediate second restart held at 236, confirming that a stable report no longer imports another random subset.
- Dogfood exposed an empty-thread recovery race: an immediate complete inventory could clear a newly attached session's app-server presence before the app-server state database exposed the thread, leaving only placeholder execution after navigating away and back. The deployed fix makes post-create and post-ensure inventory reports incremental while keeping normal complete inventory available for later cleanup. The first internal review rejected an earlier Worker grace-period implementation because its snapshot and timestamp semantics were unsound; that implementation was removed. A real empty thread was then left without a prompt: bootstrap still reported app-server presence after five seconds, and Chromium kept App-server selected both initially and after navigating to Fleet health and back, with no failed browser responses.
- The managed app-server now uses an opt-in workspace-write permission profile whose write grant is a fixed absolute Chaop repository path rather than a caller-supplied runtime workspace root. Git metadata, repository policy and skills, project Codex configuration, GitHub workflows, and the deployed connector binary remain read-only. Absolute deny globs cover nested environment, package-manager, and Cargo credential variants; dynamic workspace roots and general home-directory reads remain unavailable. Builds use a dedicated private temporary directory, cached Cargo dependencies stay offline, and test networking is loopback-only.
- The dedicated connector can now lock every managed resume, archive fallback, and turn to its configured workspace root. Together with exact read-only rules for root `AGENTS.md` and `AGENTS.override.md`, this prevents a writable nested instruction file from entering later Chaop-created thread discovery. Connector configuration rejects combining a managed listener with the external-listener bearer-token field and rejects external token files unless the configured app-server URL uses `wss://`; URL-scheme checks are consistently case-insensitive from configuration through managed-listener validation and connection setup.
- A final frozen-range review found that a bearer token sent to a plaintext loopback WebSocket could still be captured by a process that won the listener race. The connector now sends app-server bearer tokens only over certificate-validated `wss://` and supports local `unix:///absolute/path` transport. The deployment moved to a mode `0600` Unix socket inside a mode `0700` private state directory denied to managed tools, so local control no longer needs or exposes an app-server bearer token.
- App-server connection setup now applies one absolute deadline across DNS, TCP, TLS, WebSocket, and Unix-socket setup. A cancellable socket watchdog stops slow-drip handshakes at that deadline. Explicit invalid ports fail before connecting or reading an auth token. DNS uses one active capacity-bounded resolver worker; a timed-out or disconnected generation is isolated and replaced, while a two-retired-worker cap prevents repeated system-resolver stalls from accumulating threads without bound. Unix control descriptors are close-on-exec, using atomic socket flags where supported and a portable descriptor fallback on Apple targets. The release connector exposes the same bounded protocol initialisation as `--app-server-health-check` for deployment automation.
- Real managed turns created ignored canaries through the deployed Chaop command path and reached `command.finished` without an out-of-profile approval before authentication, with capability-token authentication, and after the final Unix-socket migration. Canary content and mode were verified, tracked files stayed unchanged by the turns, and the canaries were removed afterwards. The final change passed 48 script, 3 protocol, 61 Web, 294 Worker, and 192 Rust tests plus the production and release connector builds.

## Next Steps
- Use the bounded write profile for one small real source change, keeping commit/push outside the managed turn until that workflow is deliberately designed.
- Observe an idle 15-minute D1 delta after the initial import slope ages out; investigate further only if rows continue rising materially above the bounded telemetry-sample writes.
- Add retention or cleanup for stale unattached Host Sessions so rolling top-N inventory does not grow D1 indefinitely.
- Remove placeholder execution from the product flow in a later focused change once managed app-server recovery is deployed and verified.
- Keep using the documented LaunchDaemon operations for rebuild/config restarts and deliberate unloads.

## Evidence
- Private ops deployment status and connector profile.
- `chaop-agent` release build and connector doctor.
- Successful connector bootstrap response with no token value recorded here.
- Local process, loopback listener, and established Worker connection checks.
- Access-authenticated manual dogfood observations and current-day D1 rows-written telemetry.
- Read-only local app-server inventory probe for the exercised thread.
- Tracked low-cost deployed smoke with Budget Board state `normal` and measured current-day D1 rows written unchanged at 1,277.
- Read-only Wrangler D1 aggregate queries; each reported `rows_written: 0` and no connector or session identifiers were emitted.
- `pnpm test` after the review and stable-order fixes (48 script, 3 protocol, 61 web, 294 Worker, and 167 Rust tests passed).
- `pnpm build`.
- Independent Codex review of `origin/master..57304c9` found no actionable correctness issue.
- Two adjacent LaunchDaemon restarts restored the loopback app-server listener and Worker connection; the second retained the same 236-session D1 aggregate.
- The no-prompt empty-thread recovery smoke passed with app-server presence and selection retained after navigation; it sent no command.
- The final tracked deployed smoke passed direct API, asset, and real Chromium checks. Budget state remained `normal`; measured current-day D1 rows written remained 1,552 (`1.6%`) and the D1 rows-read bottleneck was `4.3%`.
- The post-write deployed smoke passed direct API, assets, and real Chromium checks with no failed browser response. Budget state remained `normal`; sampled current-day D1 rows written remained 1,235 (`1.2%`).
- Frozen-range independent reviews identified six initial boundaries in successive profiles: a writable deployed binary, incomplete credential-pattern coverage, runtime-root write expansion, an unauthenticated loopback control listener, writable isolated-review state, and bearer-token exposure to a process that won a plaintext listener race. Later ops reviews also found a Wrangler template naming collision, incomplete arbitrary-environment suffix coverage, and a writable ancestor of the deployed binary. The final profile uses a fixed absolute source write root, makes the whole repository `target` subtree read-only, redirects Cargo output to a dedicated private target, denies every `.dev.vars.*` credential, protects `.codex-tmp` and the connector-state directory, and controls the local app-server through a private Unix socket. The placeholder template was renamed to `dev.vars.example` outside Wrangler's credential namespace. Exact-profile probes verified source/template and dedicated-target writes while arbitrary environment credentials, repository-target rename, and the private socket remain unavailable. The reloaded LaunchDaemon exposes the final profile and Unix endpoint in its running app-server arguments, and a deployed control-plane turn completed through that socket.
- The final post-authentication skip-browser deployed smoke returned `200` for health, bootstrap, usage summary, and both referenced assets. Budget state remained `normal`; measured current-day D1 rows written were 1,859 (`1.9%`) and the D1 rows-read bottleneck was `2.2%`.
- The post-migration skip-browser smoke returned `200` for the same five checks, followed by a real Unix-socket managed turn with lifecycle `command.accepted -> command.started -> command.output -> command.finished` and a verified mode `0600` canary. Budget state remained `normal`; measured current-day D1 rows written were 2,195 (`2.2%`) and the D1 rows-read bottleneck was `3.0%`.
- The final review found two further configuration boundaries: managed health checks could inherit an incompatible external listener token, and a writable nested project-instruction file could affect a later thread started below the repository root. Config loading now rejects the token combination, and an opt-in managed CWD lock pins resume and turn requests to the protected root. Unit and protocol-level tests cover both behaviours, while an exact-profile `codex sandbox` probe confirms that root `AGENTS.override.md` creation is denied and an ordinary workspace write still succeeds.
- After the release connector and final profile were loaded through the system LaunchDaemon, a real Chaop write-path smoke attached an existing Host Session whose recorded cwd was nested below the repository. Its lifecycle reached `command.accepted -> command.started -> command.output -> command.finished`, and a canary written by the managed turn contained the repository root as the actual `pwd`. The smoke then detached the Host Session, archived its temporary task, and removed all local artefacts. Follow-up API/asset smoke stayed green; Budget state remained `normal`, measured current-day D1 writes remained 149 (`0.1%`), and the current four-hour local usage-window constraint was `1.1%`.
- The next full-range review found that external WebSocket authentication could follow a redirect with its bearer token, that DNS/TCP/TLS/WebSocket connection setup was not wholly bounded by the configured timeout, and that the rollout-based Host Session ensure path still requested its historic nested cwd before a later turn was locked. External connections now resolve and connect under one deadline, configure socket timeouts before TLS and WebSocket handshakes, and perform a single no-redirect handshake. The ensure fallback now uses and reports the protected workspace root when CWD locking is enabled. Focused tests cover redirect refusal, DNS and TLS timeout enforcement, and rollout-CWD override.
