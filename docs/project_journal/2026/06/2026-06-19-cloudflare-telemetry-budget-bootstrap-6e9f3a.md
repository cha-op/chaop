---
id: 20260619-6e9f3a
title: Cloudflare Telemetry Budget Bootstrap
status: completed
created: 2026-06-19
updated: 2026-06-19
branch: wip/app-server-attach-resume
pr:
supersedes:
superseded_by:
---

[ British English | [简体中文](2026-06-19-cloudflare-telemetry-budget-bootstrap-6e9f3a.zh-Hans.md) ]

# Cloudflare Telemetry Budget Bootstrap

## Summary
- Budget Board can now bootstrap current D1 usage windows with zero-count samples so quiet deployments do not show D1 rows-written as missing.
- The API Worker can optionally sample current-day Cloudflare GraphQL Analytics for Worker requests, Durable Object request-equivalent usage, D1 rows read, and D1 rows written.
- Runtime telemetry uses a separate `CF_TELEMETRY_API_TOKEN` Worker secret and non-secret selector vars generated by the API deploy script.

## Implementation Notes
- `POST /api/budget/bootstrap` is Browser-authenticated, origin-checked, and writes only the current `daily`, `four_hour`, and `burst` usage windows.
- Cloudflare telemetry is best-effort and short-timeout; GraphQL failures keep affected constraints as `missing` rather than failing Browser reads.
- Durable Object periodic incoming WebSocket messages are only folded into request equivalents when `CF_TELEMETRY_DO_NAMESPACE_NAME` scopes the metric to the Chaop namespace.
- The Budget Board compact posture continues to use the sampled hard constraint with the minimum remaining ratio.
- User-facing deployment and cost documentation explain the optional Cloudflare token permission and preserve the generic, non-instance-value requirement.

## Validation
- `pnpm --filter @chaop/worker typecheck`
- `pnpm --filter @chaop/web test`
- `pnpm --filter @chaop/worker test`
- `pnpm test`
- `pnpm build`
- `git diff --check`

## Next Steps
- Deploy the API Worker after writing a read-only `CF_TELEMETRY_API_TOKEN` secret.
- Use Budget Board `Bootstrap` once after deploy if current D1 windows are missing because the control plane has been quiet.
